<%= simple_forms_for [ @cocktail, @dose ] do |f| %>
      <%= f.label “Description” %>
      <%= f.text_field :description %>
      <%= f.association :ingredient %>
      <%= f.submit “Submit”, class:“btn btn-primary” %>
    <% end %>
    <br>
    
<%= link_to “Go back to Cocktails”, cocktails_path  %>